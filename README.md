# java21-crypto-wallet

## 錢包 MVP

```
總覽
│
├── 1️⃣ 錢包與金鑰管理 (Wallet & Key Management)
│   ├─ 功能概述
│   │   ├─ 創建錢包 (助記詞 / 私鑰)
│   │   ├─ 導入錢包 (助記詞 / 私鑰 / Keystore)
│   │   ├─ 本地加密存儲 (手機/桌面) 或 KMS (伺服器端)
│   │   └─ 多錢包/多地址管理
│   ├─ 實作細項
│   │   ├─ 助記詞生成：BIP39 (語言、字數 12/24)
│   │   ├─ 私鑰派生：BIP32/BIP44 路徑（m/44'/60'/0'/0/n）
│   │   ├─ Keystore JSON 支援 (web3 keystore v3)
│   │   ├─ 本地加密方式：AES-256-GCM，PBKDF2 / Argon2 密碼衍生
│   │   ├─ KMS：AWS KMS / Azure Key Vault / HashiCorp Vault（伺服器簽名或保管冷錢包）
│   │   └─ 安全操作：不在日誌打印私鑰、傳輸用 TLS
│   ├─ API / UI
│   │   ├─ createWallet(password, language) -> {mnemonicHash, addressMeta}
│   │   ├─ importWallet(type, payload, password)
│   │   ├─ exportKeystore(address, password)
│   │   └─ listWallets() / setActiveWallet(addr)
│   ├─ DB Schema 建議
│   │   ├─ wallets(id, userId, address, keystoreEncrypted, meta, createdAt)
│   │   └─ addresses(id, walletId, chain, derivationIndex, createdAt)
│   ├─ 安全 & 風控
│   │   ├─ 強制 PIN / 密碼 / biometrics（Client）
│   │   ├─ 助記詞在建立後僅顯示一次（提示用戶備份）
│   │   └─ 勒索/備份建議（離線、印紙、硬件錢包）
│   └─ 測試
│       ├─ 助記詞導入導出互通測試
│       ├─ 恶意輸入（錯誤助記詞、損壞 Keystore）處理測試
│       └─ 回滾與恢復流程測試
│
├── 2️⃣ 地址與多鏈管理 (Address & Chain)
│   ├─ 功能概述
│   │   ├─ 每鏈多地址支持（EVM chains 起初：ETH/BSC/Polygon）
│   │   └─ 地址展示、複製、QR Code
│   ├─ 實作細項
│   │   ├─ addressMeta：chainId、tokenStandard（ERC20/BEP20）、memo/tag（如需要）
│   │   ├─ 為每個用戶/錢包生成唯一充值地址（可 reuse 或 per-deposit 地址）
│   │   ├─ QR Code (地址、附帶 amount/memo URI)
│   │   └─ 地址格式驗證器 (per-chain)
│   ├─ API
│   │   ├─ getDepositAddress(userId, chain, token) -> address
│   │   ├─ createNewAddress(userId, chain) -> address
│   │   └─ validateAddress(chain, address) -> bool
│   ├─ DB Schema
│   │   └─ deposit_addresses(id, userId, chain, address, reservedForTxId, createdAt)
│   └─ 測試
│       ├─ 地址碰撞測試／地址重用策略測試
│       └─ QR URI edge cases（含 memo）
│
├── 3️⃣ 充值監控 (Deposit Monitoring)
│   ├─ 功能概述
│   │   ├─ 監控原生幣與代幣入金（單地址或多地址）
│   │   └─ 狀態更新：未確認 → 確認中 → 已完成
│   ├─ 實作細項
│   │   ├─ 資料來源：Public RPC/WS（Infura/Alchemy/QuickNode）或自建節點
│   │   ├─ 監控方式：WebSocket event 或 block polling（雙備援）
│   │   ├─ ERC20：監聽 Transfer event；原生幣：遍歷 block.transactions 檢查 tx.to
│   │   ├─ 交易去重：按 txHash 唯一鎖（資料庫唯一索引）
│   │   ├─ 確認數邏輯：confirmations = currentBlock - txBlock，依 chain/token 設定閾值
│   │   ├─ 最小入金額過濾（小於 minDeposit 不計入或標註）
│   │   └─ 大額 / 可疑入金觸發人工審核
│   ├─ API / 事件
│   │   ├─ internal event: NEW_DEPOSIT(txHash, userId, amount, token, blockNumber)
│   │   ├─ internal event: DEPOSIT_CONFIRMED(txHash)
│   │   ├─ GET /deposits?userId=... (前端查詢)
│   │   └─ WebSocket 推送 depositStatus 更新
│   ├─ DB Schema
│   │   └─ deposits(txHash, userId, address, chain, token, amount, txBlock, status, confirmations, createdAt, updatedAt)
│   ├─ 運維 & 高可用
│   │   ├─ 自動重連 WebSocket；閾值達不到時 fallback to polling
│   │   ├─ 多節點冗餘（兩個 RPC provider 輪替）
│   │   └─ 監控指標：平均入金延遲、未確認超時數
│   └─ 測試
│       ├─ 區塊回滾（reorg）模擬：已確認尚未達到安全數的回滾處理
│       └─ 同一 txHash 重複事件去重測試
│
├── 4️⃣ 提幣 (Withdraw)
│   ├─ 功能概述
│   │   ├─ 用戶向任意地址提幣（支援同鏈轉出）
│   │   ├─ 手續費計算、Gas management、nonce 管理
│   │   └─ 多重驗證（OTP/2FA/Email）
│   ├─ 實作細項
│   │   ├─ 提幣申請流程：createWithdrawRequest(userId, toAddress, chain, token, amount)
│   │   ├─ 檢查：餘額、最小提款額、whitelist/blacklist
│   │   ├─ 風控：單筆/日限額、KYC 等級限制、大額人工審核
│   │   ├─ Gas/fee 策略：用戶付 gas 或平台代付（顯示估價）
│   │   ├─ 交易簽名策略：
│   │   │   ├─ 客戶端簽名（non-custodial）
│   │   │   └─ 伺服器簽名（custodial, 私鑰由 KMS 管理）
│   │   ├─ nonce 管理（避免重放或 nonce 衝突）
│   │   └─ 發送交易後狀態跟蹤（pending → confirmed）
│   ├─ API
│   │   ├─ POST /withdraws (申請)
│   │   ├─ POST /withdraws/{id}/confirm (OTP/2FA)
│   │   ├─ GET /withdraws/{id} (狀態查詢)
│   │   └─ admin APIs（審核/取消）
│   ├─ DB Schema
│   │   └─ withdraws(id, userId, toAddress, chain, token, amount, fee, nonce, txHash, status, createdAt, updatedAt)
│   └─ 測試
│       ├─ nonce race condition 模擬（高併發提款）
│       └─ 用戶取消、重試流程測試
│
├── 5️⃣ 法幣通路：On-ramp / Off-ramp
│   ├─ On-ramp（法幣→加密）
│   │   ├─ 支援信用卡、銀行轉帳、第三方支付（Stripe、MoonPay、Simplex、Wyre）
│   │   ├─ 即時價格與匯率計算（含手續費）
│   │   ├─ 交易追蹤：訂單、支付狀態、法幣入賬後 credit 加密資產到用戶錢包
│   │   └─ API：createFiatPurchase, getFiatStatus, webhook 接收第三方回調
│   ├─ Off-ramp（加密→法幣）
│   │   ├─ 支援售幣（在平台內撮合或使用場外/第三方出金）
│   │   ├─ 出金到 bank account / 支付通道（需要銀行接口 + KYC）
│   │   └─ 風控：AML 检查、限額、人工審核
│   └─ 合作注意
│       ├─ 第三方支付有合規/國別限制
│       ├─ 使用 webhook 並驗證簽名
│       └─ 記錄完整流水（法幣 & 幣幣）
│
├── 6️⃣ 多幣種 / 多鏈支持 (Multi-chain & Token)
│   ├─ 支援列表：ETH、BSC、Polygon（MVP）→ 再擴充 Solana/Bitcoin（非 EVM）時另行設計
│   ├─ 抽象化 chainClient 接口
│   │   ├─ getBalance(address, token)
│   │   ├─ sendTransaction(signedTx)
│   │   └─ subscribeDeposits(addresses)
│   ├─ token metadata：decimals、contractAddress、symbol、tokenStandard
│   └─ 代幣上下架流程（admin UI）
│
├── 7️⃣ 交易紀錄與對帳 (Tx History & Reconciliation)
│   ├─ 用戶端交易紀錄：type (deposit/withdraw/transfer/buy/sell)、狀態、時間戳
│   ├─ 對帳流程：鏈上 tx 與 DB 記錄比對（每日/實時）
│   ├─ 退單/補帳機制（人工作業）
│   └─ API：getTransactions(userId, filters)
│
├── 8️⃣ 安全 (Security)
│   ├─ 基礎安全
│   │   ├─ TLS everywhere
│   │   ├─ 秘密存儲（KMS、HSM）
│   │   └─ 日誌紅旗過濾（不可記錄私鑰）
│   ├─ 用戶安全
│   │   ├─ 密碼強度、PIN、Biometrics（Mobile）
│   │   ├─ 2FA / OTP / Email 確認
│   │   └─ 提幣白名單（可選）
│   ├─ 平台安全
│   │   ├─ 交易限額、風險分級
│   │   ├─ 多簽錢包方案（大額出金）
│   │   └─ DDoS / WAF 防護 & 入侵檢測
│   └─ 合規安全
│       ├─ KYC/AML 流程整合（合作商或自建）
│       └─ 事件追蹤與稽核日誌保存
│
├── 9️⃣ API 設計（前後端 & 第三方）
│   ├─ 公開 API (供前端)
│   │   ├─ Auth: /login /logout /refresh
│   │   ├─ Wallet: /wallets, /addresses, /balance
│   │   └─ Tx: /deposits, /withdraws, websocket /push events
│   ├─ 內部 API（微服務/事件）
│   │   ├─ deposit-processor、withdraw-processor、reconciliation-service
│   │   └─ message queue: Kafka / RabbitMQ for events
│   └─ 第三方 webhook：支持簽名驗證（HMAC）
│
├── 🔟 監控、日誌與告警 (Monitoring & Ops)
│   ├─ 指標 (Metrics)
│   │   ├─ deposit latency, confirmation time, txs/sec, failed tx rate
│   │   └─ RPC provider availability
│   ├─ 日誌
│   │   ├─ 交易事件（可追溯 txHash）
│   │   └─ 操作日誌（審計）
│   ├─ 告警
│   │   ├─ RPC down, high pending txs, large unexpected deposits
│   │   └─ wallet key access anomalies
│   └─ 運維工具
│       ├─ health check endpoints
│       └─ automated restart & circuit breaker for provider failures
│
├── 1️⃣1️⃣ 使用者體驗（UX）與前端要點
│   ├─ UI 要點
│   │   ├─ 明確顯示狀態：未確認/確認中/已完成/失敗
│   │   ├─ 顯示最小充值額、建議 Gas、避免錯幣提醒
│   │   └─ 歷史列表可導出（CSV）
│   ├─ 通知
│   │   ├─ 即時推送（WebSocket / Push）
│   │   └─ Email / SMS（重要事件）
│   └─ 國際化（i18n）與本地化（時間、貨幣格式）
│
├── 1️⃣2️⃣ 合規、KYC/AML（必要時）
│   ├─ KYC 分級：註冊級、交易級、出金級
│   ├─ AML 規則：黑名單地址、風險地區、交易模式檢測
│   ├─ 日誌與稽核保留策略（依國家/地區規定）
│   └─ 法務文件：TOS、Privacy Policy、Risk Disclosure
│
└── 1️⃣3️⃣ 測試與部署（CI/CD）
    ├─ 單元測試、整合測試（含 chain 模擬）
    ├─ 測試網路：使用 testnet（Goerli, BSC testnet, Polygon Mumbai）
    ├─ Load test：模擬高并发充值/提幣場景
    ├─ Canary / Blue-Green deploy，確保零停機
    └─ 災難恢復、備份（DB、Key Vault）
```